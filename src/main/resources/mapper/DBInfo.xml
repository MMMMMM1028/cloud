<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wzc.cloud.dao.DBInfoDao">
    <insert id="insertDB" parameterType="DBInfo">
        INSERT
        INTO dbinfo_(ram, cpu, version, type, port, IP, userId)
        VALUES (#{ram}, #{cpu}, #{version}, #{type}, #{port}, #{IP}, #{userId})
    </insert>
</mapper>